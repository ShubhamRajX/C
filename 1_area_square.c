#include<stdio.h>
int main()
{
    // int side = 2;
    // // int area = side*side;
    // // printf("area is %d \n",area);
    // printf("area is %d \n",side*side);
    // return 0;

    int side;
    printf("Enter the value of side");
    scanf("%d",&side);

    // int area = side*side;
    // printf("area of square is %d \n",area);

    printf("area of square is %d \n",side*side);

    return 0;
}