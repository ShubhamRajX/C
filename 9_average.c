#include<stdio.h>
int main()
{
    int a,b,c;
    scanf("%d",&a);
    scanf("%d",&b);
    scanf("%d",&c);

    printf("average : %d \n",((a+b+c)/3));

    // int a = 5;
    // int b = 6;
    // (a<b)?printf("a less than b"):printf("a greater than b");
    return 0;
}